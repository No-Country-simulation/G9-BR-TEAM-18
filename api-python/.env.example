# Groq API Key — usada como fallback quando o modelo ML tem confiança < 80%
# Obtenha sua chave em: https://console.groq.com/keys
#
#   cp .env.example .env
#
# Se não configurada, a API funciona apenas com o modelo ML + regras.
GROQ_API_KEY=your_groq_api_key_here
