# =============================================================================
# EnergIAI - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# --- API URLs ---
VITE_API_URL=http://localhost:8080
ML_SERVICE_URL=http://ml-service:8000

# --- Server Ports ---
SERVER_PORT=8080
ML_SERVICE_PORT=8000
FRONTEND_PORT=5173

# =============================================================================
# Database — Oracle ATP
# =============================================================================
# Substitua pelos valores do seu ambiente Oracle Cloud ATP
SPRING_DATASOURCE_URL=jdbc:oracle:thin:@(description=...)
SPRING_DATASOURCE_DRIVER_CLASS_NAME=oracle.jdbc.OracleDriver
SPRING_DATASOURCE_USERNAME=ADMIN
SPRING_DATASOURCE_PASSWORD=your_password_here
SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.OracleDialect
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
SPRING_FLYWAY_ENABLED=true
SPRING_FLYWAY_LOCATIONS=classpath:db/migration/oracle
SPRING_FLYWAY_DEFAULT_SCHEMA=ADMIN
SPRING_FLYWAY_BASELINE_ON_MIGRATE=true
SPRING_FLYWAY_BASELINE_VERSION=0
SPRING_FLYWAY_URL=${SPRING_DATASOURCE_URL}
SPRING_FLYWAY_USER=${SPRING_DATASOURCE_USERNAME}
SPRING_FLYWAY_PASSWORD=${SPRING_DATASOURCE_PASSWORD}
SPRING_JPA_SHOW_SQL=false
# ORACLE_WALLET_LOCATION=/etc/secrets/Wallet_DBNAME

# --- ML Service ---
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
MODEL_PATH=categorization-model.joblib
TRAINING_LOG_PATH=treino_feedback.jsonl
GROQ_MAX_RPM=25
GROQ_MAX_RPD=900

# --- Business Configuration ---
KWH_TARIFF=0.75
CO2_EMISSION_FACTOR=0.096

# --- CORS & Security ---
CORS_ALLOWED_ORIGINS=*
JWT_SECRET=default-dev-secret-key-change-in-production-32chars!
JWT_EXPIRATION_MS=604800000
SESSION_MAX_AGE_SECONDS=604800
SESSION_SECURE=false

# --- ML Service — Anti-Corruption Layer (ACL) field mappings ---
ML_OUTPUT_FIELD_CATEGORY=category
ML_OUTPUT_FIELD_PROBABILITY=probability
ML_OUTPUT_FIELD_RECOMMENDATIONS=recommendations

# --- ML Service — Schema Discovery default categories ---
ML_DEFAULT_CATEGORIES=EXCELENTE,BOM,MEDIANO,RUIM,CRITICO

# --- ML Training ---
RANDOM_SEED=42
N_SYNTHETIC=4000
FEEDBACK_WEIGHT=5
